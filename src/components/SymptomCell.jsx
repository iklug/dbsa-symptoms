
const SymptomCell = ({name}) => {
    return (
        <div className=" text-gray-400 text-[.65rem] w-24 h-8 flex flex-col justify-center border border-l-transparent border-gray-200 shrink-0">{name}</div>
    )
}

export default SymptomCell;